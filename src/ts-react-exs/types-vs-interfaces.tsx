// Rule of Thumb

// use interface for public API's definition when authoring a library or 3rd party ambient type definitions

// use type for React Component Props and State, for consistency (because they are more constrained)