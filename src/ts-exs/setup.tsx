// Install TS globally on your machine
//  $ npm i -g typescript

// Check Version
// $ tsc -v

// Create the tsconfig.json file
// $ tsc --init

// Set the root (to compile TS files from) and ouput (for the compiled JS files) directories in tsconfig.json
// "rootDir": "./src"
// "outDir": "./public"

